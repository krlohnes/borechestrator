{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "borechestrator.serviceAccountName" . }}
  labels:
    {{- include "borechestrator.labels" . | nindent 4 }}
{{- end }}
